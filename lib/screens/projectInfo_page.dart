import 'package:flutter/material.dart';

class ProjectInfo extends StatelessWidget {
  const ProjectInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Về Đồ án"),
      ),
      body: Column(
        children: <Widget>[Expanded(child: Text("tr"))],
      ),
    );
  }
}
